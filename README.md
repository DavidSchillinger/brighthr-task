To run the application locally: `pnpm i && pnpm dev` should just work assuming `corepack` is set up.

`npm` should work too, but you probably won't get the exact same versions of all dependencies as me.
